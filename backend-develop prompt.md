# 流动摊位共享平台管理端后端开发提示词

## 项目背景
你需要为流动摊位共享平台开发管理端后端API，前端已经完成开发，现在需要实现后端接口来支持前端功能。

## 技术要求

### 后端框架选择
请选择以下任一技术栈进行开发：
- **推荐**：Spring Boot 3.x + MyBatis-Plus + MySQL 8.0
- **备选**：Node.js + Express/Koa + MySQL
- **备选**：PHP 8.x + Laravel + MySQL
- **备选**：Python 3.x + FastAPI + MySQL

### 核心技术要求
1. **数据库**：MySQL 8.0+
2. **认证**：JWT Token
3. **API文档**：Swagger/OpenAPI
4. **数据验证**：参数校验、数据验证
5. **异常处理**：统一异常处理机制
6. **日志记录**：操作日志、错误日志
7. **文件上传**：支持图片上传到本地或云存储
8. **数据导出**：支持Excel导出功能
9. **分页查询**：所有列表接口都支持分页
10. **跨域处理**：CORS配置

## API开发规范

### 1. 接口路径规范
```
基础路径：/api/admin
示例：/api/admin/users、/api/admin/vendors
```

### 2. 请求头规范
```
Content-Type: application/json
Authorization: Bearer {token}
```

### 3. 统一响应格式
```json
{
  "code": 200,
  "message": "操作成功",
  "data": {}
}
```

### 4. 错误响应格式
```json
{
  "code": 400,
  "message": "错误信息",
  "data": null
}
```

### 5. 错误码定义
- 200：操作成功
- 400：请求参数错误
- 401：未授权，需要登录
- 403：无权限访问
- 404：资源不存在
- 500：服务器内部错误

## 功能模块开发要求

### 模块1：认证模块
**接口清单**：
1. POST /api/admin/login - 管理员登录
2. POST /api/admin/logout - 管理员登出

**开发要求**：
- 密码使用BCrypt加密存储
- 登录成功返回JWT Token
- Token有效期建议24小时
- 登出时将Token加入黑名单（可选）

### 模块2：用户管理模块
**接口清单**：
1. GET /api/admin/users - 获取用户列表（支持分页、搜索、筛选）
2. PUT /api/admin/user/{id}/status - 更新用户状态
3. DELETE /api/admin/user/{id} - 删除用户
4. GET /api/admin/users/export - 导出用户数据（Excel）

**开发要求**：
- 支持按昵称、手机号搜索
- 支持按状态筛选（活跃、不活跃、封禁）
- 支持按时间范围筛选（近7天、30天、90天、一年）
- 分页参数：page、pageSize
- 删除操作使用软删除（添加deleted_at字段）
- 导出功能使用Apache POI或类似库

### 模块3：摊主管理模块
**接口清单**：
1. GET /api/admin/vendors - 获取摊主列表（支持分页、搜索、筛选）
2. PUT /api/admin/vendor/{id}/status - 审核摊主
3. GET /api/admin/vendor/{id} - 查看摊主详情
4. DELETE /api/admin/vendor/{id} - 删除摊主
5. GET /api/admin/vendors/export - 导出摊主数据（Excel）

**开发要求**：
- 支持按摊主名称、手机号搜索
- 支持按状态筛选（活跃、不活跃、封禁、待审核）
- 支持按时间范围筛选
- 审核功能需要记录审核备注
- 摊主详情包含摊主信息和摊位列表

### 模块4：摊位管理模块
**接口清单**：
1. GET /api/admin/stalls - 获取摊位列表（支持分页、搜索、筛选）
2. PUT /api/admin/stall/{id}/status - 审核摊位
3. GET /api/admin/stall/{id} - 查看摊位详情
4. DELETE /api/admin/stall/{id} - 删除摊位
5. GET /api/admin/stalls/export - 导出摊位数据（Excel）

**开发要求**：
- 支持按摊位名称、地址搜索
- 支持按状态筛选（活跃、不活跃、封禁、待审核）
- 支持按分类筛选（美食小吃、水果生鲜、日用百货、其他）
- 支持按时间范围筛选
- 摊位详情包含摊位信息和图片列表

### 模块5：评价管理模块
**接口清单**：
1. GET /api/admin/reviews - 获取评价列表（支持分页、搜索、筛选）
2. DELETE /api/admin/review/{id} - 删除评价
3. DELETE /api/admin/reviews/batch - 批量删除评价
4. GET /api/admin/reviews/export - 导出评价数据（Excel）

**开发要求**：
- 支持按评价内容搜索
- 支持按评分筛选（1-5星）
- 支持按状态筛选（正常、已举报、已删除）
- 支持按时间范围筛选
- 批量删除功能需要事务处理

### 模块6：统计数据模块
**接口清单**：
1. GET /api/admin/stats - 获取统计数据
2. GET /api/admin/stats/user-growth - 获取用户增长趋势

**开发要求**：
- 统计数据包含：总用户数、总摊主数、总摊位数、待审核数
- 用户增长趋势支持按周、月、年查询
- 建议使用Redis缓存统计数据
- 分类统计数据需要实时计算

### 模块7：系统设置模块
**接口清单**：
1. GET /api/admin/settings - 获取系统设置
2. PUT /api/admin/settings - 更新系统设置

**开发要求**：
- 设置分为三个分类：基础设置、安全设置、通知设置
- 使用JSON格式存储配置信息
- 更新设置后需要清除相关缓存
- 敏感信息（如API密钥）需要加密存储

## 数据库设计要求

### 表结构设计
请按照以下表结构创建数据库表：

1. **admin表** - 管理员表
2. **user表** - 用户表
3. **vendor表** - 摊主表
4. **stall表** - 摊位表
5. **review表** - 评价表
6. **system_settings表** - 系统设置表

### 字段要求
- 所有表都需要：id、created_at、updated_at字段
- 删除操作使用软删除：deleted_at字段
- 状态字段使用tinyint类型
- 时间字段使用datetime类型
- 字符串字段使用varchar类型
- 文本字段使用text类型
- JSON字段使用json类型（MySQL 5.7+）

### 索引要求
- 主键：id字段
- 唯一索引：username、phone、wechat_openid等唯一字段
- 普通索引：status、category、created_at等常用查询字段
- 外键索引：vendor_id、user_id、stall_id等关联字段

## 开发流程要求

### 1. 项目初始化
- 创建项目结构
- 配置数据库连接
- 配置日志系统
- 配置跨域处理
- 配置文件上传

### 2. 基础功能开发
- 实现统一响应格式
- 实现统一异常处理
- 实现JWT认证拦截器
- 实现参数验证

### 3. 业务模块开发
按照以下顺序开发：
1. 认证模块
2. 用户管理模块
3. 摊主管理模块
4. 摊位管理模块
5. 评价管理模块
6. 统计数据模块
7. 系统设置模块

### 4. 接口测试
- 使用Postman或Swagger测试所有接口
- 测试正常流程和异常流程
- 测试参数验证和错误处理
- 测试分页和筛选功能

### 5. 文档编写
- 生成Swagger API文档
- 编写接口使用说明
- 编写数据库设计文档
- 编写部署文档

## 代码质量要求

### 1. 代码规范
- 遵循所选技术栈的代码规范
- 使用有意义的变量名和函数名
- 添加必要的注释
- 保持代码简洁清晰

### 2. 异常处理
- 捕获所有可能的异常
- 返回友好的错误信息
- 记录详细的错误日志
- 不暴露敏感信息

### 3. 安全要求
- 密码加密存储
- SQL注入防护
- XSS攻击防护
- CSRF防护
- 接口权限验证

### 4. 性能优化
- 使用数据库连接池
- 使用Redis缓存热点数据
- 分页查询避免全表扫描
- 使用索引优化查询性能

## 测试要求

### 1. 单元测试
- 对核心业务逻辑编写单元测试
- 测试覆盖率达到80%以上

### 2. 集成测试
- 测试接口的完整流程
- 测试异常情况处理
- 测试并发场景

### 3. 性能测试
- 测试接口响应时间
- 测试并发处理能力
- 测试数据库查询性能

## 部署要求

### 1. 环境配置
- 开发环境配置
- 测试环境配置
- 生产环境配置

### 2. 部署方式
- 支持Docker部署
- 支持传统部署
- 提供部署文档

### 3. 监控要求
- 接口响应时间监控
- 错误率监控
- 系统资源监控

## 交付要求

### 1. 代码交付
- 完整的源代码
- 配置文件示例
- 数据库脚本

### 2. 文档交付
- API接口文档（Swagger）
- 数据库设计文档
- 部署文档
- 使用说明文档

### 3. 测试交付
- 测试用例
- 测试报告

## 开发时间估算

- 项目初始化：1-2天
- 基础功能开发：2-3天
- 业务模块开发：5-7天
- 接口测试：2-3天
- 文档编写：1-2天
- **总计：11-17天**

## 注意事项

1. **前后端对接**：严格按照API设计文档开发，确保与前端完全兼容
2. **数据格式**：所有时间格式统一使用 `YYYY-MM-DD HH:mm:ss`
3. **分页参数**：page从1开始，pageSize默认为10
4. **状态码**：严格按照错误码定义返回
5. **敏感信息**：不要在日志中记录密码、Token等敏感信息
6. **版本控制**：使用Git进行版本控制
7. **代码审查**：定期进行代码审查
8. **持续集成**：建议使用CI/CD工具

## 常见问题

### Q1：如何处理文件上传？
A：使用MultipartFile接收文件，保存到本地或云存储，返回文件URL。

### Q2：如何处理分页？
A：使用LIMIT和OFFSET实现分页，或者使用MyBatis-Plus的Page对象。

### Q3：如何处理Excel导出？
A：使用Apache POI（Java）、exceljs（Node.js）或类似库生成Excel文件。

### Q4：如何处理JWT认证？
A：登录时生成Token，后续请求在Header中携带Token，拦截器验证Token有效性。

### Q5：如何处理软删除？
A：添加deleted_at字段，删除时更新该字段，查询时过滤已删除记录。

## 联系方式

如有问题，请参考以下资源：
- API设计文档：backend-api-design.md
- 前端代码：frontend目录
- 数据库设计：见API设计文档

## 开始开发

请按照以上要求开始开发，确保：
1. 严格按照API设计文档实现接口
2. 确保与前端完全兼容
3. 保证代码质量和安全性
4. 提供完整的文档和测试

祝你开发顺利！