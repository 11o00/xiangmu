server:
  port: 3000

spring:
  application:
    name: stall-map-backend
  datasource:
    url: ${DB_URL:jdbc:mysql://test-db-mysql.ns-iqkda4e7.svc:3306/stall_map?useUnicode=true&characterEncoding=utf8&useSSL=false&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true}
    username: ${DB_USER:root}
    password: ${DB_PASS:6t2ltdlf}
    driver-class-name: com.mysql.cj.jdbc.Driver

mybatis-plus:
  configuration:
    map-underscore-to-camel-case: true
  global-config:
    db-config:
      id-type: assign_id

logging:
  level:
    com.stallmap: info

